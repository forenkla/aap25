# This is the AAP growth installer inventory file
# Please consult the docs if you're unsure what to add
# For all optional variables please consult the included README.md
# or the Red Hat documentation:
# https://docs.redhat.com/en/documentation/red_hat_ansible_automation_platform/2.5/html/containerized_installation

# This section is for your AAP Gateway host(s)
# -----------------------------------------------------
[automationgateway]
'{{ aap_fqdn }}'

# This section is for your AAP Controller host(s)
# -----------------------------------------------------
[automationcontroller]
'{{ aap_fqdn }}'

# This section is for your AAP Automation Hub host(s)
# -----------------------------------------------------
[automationhub]
'{{ aap_fqdn }}'

# This section is for your AAP EDA Controller host(s)
# -----------------------------------------------------
[automationeda]
'{{ aap_fqdn }}'

# This section is for the AAP database
# -----------------------------------------------------
[database]
'{{ aap_fqdn }}'

#[execution_nodes]


[all:vars]
ansible_connection=local

# Common variables
# https://docs.redhat.com/en/documentation/red_hat_ansible_automation_platform/2.5/html/containerized_installation/appendix-inventory-files-vars#ref-general-inventory-variables
# -----------------------------------------------------
bundle_install=true
# The bundle directory must include /bundle in the path
bundle_dir='{{ aap_installer_dir }}/bundle'

ca_tls_cert='{{ cert_dir }}/fullchain.pem'
ca_tls_key='{{ cert_dir }}/privkey.pem'

postgresql_admin_username=postgres
postgresql_admin_password='{{ simple_password }}'

registry_username='{{ registry_username }}'
registry_password='{{ registry_password }}'

redis_mode=standalone

# AAP Gateway
# https://docs.redhat.com/en/documentation/red_hat_ansible_automation_platform/2.5/html/containerized_installation/appendix-inventory-files-vars#ref-gateway-variables
# -----------------------------------------------------
gateway_admin_password='{{ simple_password }}'
gateway_pg_host='{{ aap_fqdn }}'
gateway_pg_password='{{ simple_password }}'

gateway_tls_cert='{{ cert_dir }}/fullchain.pem'
gateway_tls_key='{{ cert_dir }}/privkey.pem'


# AAP Controller
# https://docs.redhat.com/en/documentation/red_hat_ansible_automation_platform/2.5/html/containerized_installation/appendix-inventory-files-vars#ref-controller-variables
# -----------------------------------------------------
controller_admin_password='{{ simple_password }}'
controller_pg_host='{{ aap_fqdn }}'
controller_pg_password='{{ simple_password }}'

controller_tls_cert='{{ cert_dir }}/fullchain.pem'
controller_tls_key='{{ cert_dir }}/privkey.pem'

# AAP Automation Hub
# https://docs.redhat.com/en/documentation/red_hat_ansible_automation_platform/2.5/html/containerized_installation/appendix-inventory-files-vars#ref-hub-variables
# -----------------------------------------------------
hub_admin_password='{{ simple_password }}'
hub_pg_host='{{ aap_fqdn }}'
hub_pg_password='{{ simple_password }}'

# hub_extra_settings=[{"setting": "GALAXY_ENABLE_UNAUTHENTICATED_COLLECTION_ACCESS", "value": True}, {"setting": "GALAXY_ENABLE_UNAUTHENTICATED_COLLECTION_DOWNLOAD", "value": True}]

hub_tls_cert='{{ cert_dir }}/fullchain.pem'
hub_tls_key='{{ cert_dir }}/privkey.pem'

# AAP EDA Controller
# https://docs.redhat.com/en/documentation/red_hat_ansible_automation_platform/2.5/html/containerized_installation/appendix-inventory-files-vars#event-driven-ansible-controller
# -----------------------------------------------------
eda_admin_password='{{ simple_password }}'
eda_pg_host='{{ aap_fqdn }}'
eda_pg_password='{{ simple_password }}'

eda_tls_cert='{{ cert_dir }}/fullchain.pem'
eda_tls_key='{{ cert_dir }}/privkey.pem'