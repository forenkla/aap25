---
ansible-navigator:
  ansible:
    config:
      path: ansible.cfg
  editor:
    command: vi
  execution-environment:
    container-engine: podman
    enabled: true
    image: automation-platform-aap.apps.hyper.jboivie.se/ee-supported-rhel9
  logging:
    append: true
    level: info
  playbook-artifact:
    enable: false
