---
ansible-navigator:
  ansible:
    config:
      path: ansible.cfg
  editor:
    command: vi
  execution-environment:
    container-engine: podman
    enabled: true
    image: aap25.containersarelinux.net/ansible-automation-platform-25/aap-configurator
  logging:
    append: true
    level: info
  playbook-artifact:
    enable: false
