---
- name: "Install System Role for Web Console"
  hosts: "{{ vm_name }}"
  become: yes

  tasks:
    - name: Start Cockpit via cmd
      ansible.builtin.command: systemctl start cockpit

    - name: Open Firewall for Web Console
      firewalld:
        service: cockpit
        permanent: yes
        immediate: yes
        state: enabled
